---
import Layout from '../components/Layout.astro';
import { services } from '../data/services';
import { courses } from '../data/courses';
---

<Layout title="Servicios y Cursos" description="Explora nuestros servicios de capacitación digital y tecnológica">
  <section class="bg-primary-50 py-16">
    <div class="container mx-auto px-4">
      <div class="text-center mb-12">
        <h1 class="text-4xl font-bold text-primary-800 mb-4">
          Nuestros Servicios y Cursos
        </h1>
        <p class="text-xl text-primary-600 max-w-3xl mx-auto">
          Diseñamos soluciones de capacitación tecnológica adaptadas a tus necesidades. Desde alfabetización digital hasta estrategias de transformación empresarial.
        </p>
      </div>

      {/* Servicios */}
      <section id="servicios" class="mb-16">
        <h2 class="text-3xl font-bold text-primary-800 text-center mb-10">
          Servicios Especializados
        </h2>
        <div class="grid md:grid-cols-3 gap-8">
          {services.map((service, index) => (
            <div 
              id={`service-${service.id}`} 
              class="bg-white p-8 rounded-xl shadow-soft hover:shadow-medium transition-all duration-300 group"
            >
              <div class="flex justify-center mb-6">
                <div class="w-20 h-20 bg-accent-100 text-accent-600 rounded-full flex items-center justify-center group-hover:bg-accent-200 transition-colors">
                  <svg class="w-10 h-10" fill="currentColor" viewBox="0 0 20 20">
                    {service.icon === 'UserIcon' && (
                      <path d="M10 9a3 3 0 100-6 3 3 0 000 6zm-7 9a7 7 0 1114 0H3z" />
                    )}
                    {service.icon === 'LaptopIcon' && (
                      <path d="M2 5a2 2 0 012-2h12a2 2 0 012 2v10a2 2 0 01-2 2H4a2 2 0 01-2-2V5zm3.5 1a.5.5 0 00-.5.5v1a.5.5 0 00.5.5h2a.5.5 0 00.5-.5v-1a.5.5 0 00-.5-.5h-2zm5 0a.5.5 0 00-.5.5v1a.5.5 0 00.5.5h5a.5.5 0 00.5-.5v-1a.5.5 0 00-.5-.5h-5zM9 8.5a.5.5 0 01.5-.5h5a.5.5 0 010 1h-5A.5.5 0 019 8.5zm-4 3a.5.5 0 01.5-.5h8a.5.5 0 010 1h-8a.5.5 0 01-.5-.5zm5 3a.5.5 0 01.5-.5h3a.5.5 0 010 1h-3a.5.5 0 01-.5-.5z" />
                    )}
                    {service.icon === 'GlobeIcon' && (
                      <path d="M8.433 7.418c.155-.103.346-.196.567-.267v1.536a2.573 2.573 0 01-.567-.267C8.07 8.34 8 8.007 8 8c0-.007.07-.34.433-.582zM11 9.234l-.553.392A.773.773 0 0110 9.607v-1.175a1.99 1.99 0 00.897.4c.045.18.13.353.258.514L11 9.234zM10 2a8 8 0 100 16 8 8 0 000-16zm0 13.5a7.496 7.496 0 01-1.519-.166c.085-.16.242-.343.437-.543l.597-.68.754-.902a.53.53 0 00.09-.29v-.79a.548.548 0 00-.21-.44.62.62 0 00-.412-.17h-2.5a.569.569 0 00-.428.192l-.492.56v.777a.635.635 0 00.094.352c.117.195.258.373.425.525l-1.564.607A7.5 7.5 0 1110 15.5z" />
                    )}
                  </svg>
                </div>
              </div>
              <h3 class="text-2xl font-semibold text-primary-800 text-center mb-4">
                {service.title}
              </h3>
              <p class="text-primary-600 text-center mb-6">
                {service.description}
              </p>
              <ul class="space-y-3 text-primary-700 mb-6">
                {service.benefits.map((benefit) => (
                  <li class="flex items-center">
                    <svg class="w-5 h-5 mr-3 text-accent-500" fill="currentColor" viewBox="0 0 20 20">
                      <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
                    </svg>
                    {benefit}
                  </li>
                ))}
              </ul>
              <div class="text-center">
                <a 
                  href={`/contact?servicio=${service.id}`} 
                  class="btn btn-primary px-6 py-3 w-full"
                >
                  Solicitar Servicio
                </a>
              </div>
            </div>
          ))}
        </div>
      </section>

      {/* Cursos */}
      <section id="cursos" class="bg-white rounded-xl p-8 shadow-soft">
        <h2 class="text-3xl font-bold text-primary-800 text-center mb-10">
          Catálogo de Cursos
        </h2>
        <div class="grid md:grid-cols-3 gap-8">
          {courses.map((course) => (
            <div class="bg-primary-50 rounded-xl overflow-hidden shadow-soft hover:shadow-medium transition-all duration-300">
              <img 
                src={course.imageUrl} 
                alt={course.title} 
                class="w-full h-48 object-cover"
              />
              <div class="p-6">
                <h3 class="text-xl font-semibold text-primary-800 mb-3">
                  {course.title}
                </h3>
                <p class="text-primary-600 mb-4">
                  {course.description}
                </p>
                <div class="flex justify-between text-sm text-primary-700 mb-4">
                  <span class="flex items-center">
                    <svg class="w-5 h-5 mr-2 text-accent-500" fill="currentColor" viewBox="0 0 20 20">
                      <path d="M10 12a2 2 0 100-4 2 2 0 000 4z" />
                      <path fill-rule="evenodd" d="M.458 10C1.732 5.943 5.522 3 10 3s8.268 2.943 9.542 7c-1.274 4.057-5.064 7-9.542 7S1.732 14.057.458 10zM14 10a4 4 0 11-8 0 4 4 0 018 0z" clip-rule="evenodd" />
                    </svg>
                    {course.level}
                  </span>
                  <span class="flex items-center">
                    <svg class="w-5 h-5 mr-2 text-accent-500" fill="currentColor" viewBox="0 0 20 20">
                      <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z" clip-rule="evenodd" />
                    </svg>
                    {course.duration}
                  </span>
                </div>
                <div>
                  <h4 class="font-medium text-primary-700 mb-2">Temas:</h4>
                  <ul class="space-y-1 text-primary-600">
                    {course.topics.map((topic) => (
                      <li class="flex items-center">
                        <svg class="w-4 h-4 mr-2 text-accent-500" fill="currentColor" viewBox="0 0 20 20">
                          <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-8.707l-3-3a1 1 0 00-1.414 1.414L10.586 9H7a1 1 0 100 2h3.586l-1.293 1.293a1 1 0 101.414 1.414l3-3a1 1 0 000-1.414z" clip-rule="evenodd" />
                        </svg>
                        {topic}
                      </li>
                    ))}
                  </ul>
                </div>
                <div class="mt-6">
                  <a 
                    href={`/contact?curso=${course.id}`} 
                    class="btn btn-secondary w-full px-6 py-3"
                  >
                    Inscribirme
                  </a>
                </div>
              </div>
            </div>
          ))}
        </div>
      </section>

      {/* Llamada a la acción */}
      <section class="bg-accent-500 text-white mt-16 rounded-xl">
        <div class="container mx-auto px-4 py-16 text-center">
          <h2 class="text-3xl font-bold mb-6">
            ¿Listo para Transformar tu Futuro Digital?
          </h2>
          <p class="text-xl mb-8 max-w-2xl mx-auto">
            Nuestros expertos están listos para guiarte en tu viaje de aprendizaje tecnológico. Personaliza tu ruta de capacitación hoy.
          </p>
          <div class="flex justify-center space-x-4">
            <a 
              href="/contact" 
              class="btn bg-white text-accent-600 hover:bg-accent-50 px-8 py-4 text-lg font-bold"
            >
              Contáctanos
            </a>
            <a 
              href="/services#cursos" 
              class="btn border-2 border-white text-white hover:bg-white hover:text-accent-500 px-8 py-4 text-lg font-bold"
            >
              Ver Todos los Cursos
            </a>
          </div>
        </div>
      </section>
    </div>
  </section>
</Layout>

<style>
  .course-card-enter {
    opacity: 0;
    transform: translateY(20px);
  }
  .course-card-enter-active {
    transition: all 0.3s ease;
    opacity: 1;
    transform: translateY(0);
  }
</style>